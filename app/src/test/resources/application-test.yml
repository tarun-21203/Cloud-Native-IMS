spring:
  datasource:
    url: jdbc:h2:mem:imstest;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  cache:
    type: none

ims:
  role: ALL
  aws:
    enabled: false
    sqs:
      enabled: false
    sns:
      enabled: false
  forecast:
    provider: ewma
